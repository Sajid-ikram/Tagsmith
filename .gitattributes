# Normalise text to LF in the repository.
* text=auto eol=lf

# The wrapper script must stay LF or it will not run on Linux or CI.
gradlew text eol=lf
gradlew.bat text eol=crlf

# Binary assets must never be line-ending converted.
*.ttf binary
*.otf binary
*.jar binary
*.png binary
*.webp binary
